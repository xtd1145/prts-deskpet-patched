{
  "name": "claude-code-but-priestess",
  "version": "0.7.9",
  "description": "PRTS desktop pet — patched complete build (auto-launch, DSH control, popover mode switch, backend 404 fix, pet window behavior, pet-side toggle, userData inheritance, auto-update, feedback, mac one-click pkg).",
  "main": "src/main/main.js",
  "private": true,
  "author": "SVAH-X (original) + patched",
  "license": "PolyForm-Noncommercial-1.0.0",
  "dependencies": {
    "electron-updater": "^6.8.3"
  },
  "devDependencies": {
    "electron": "42.3.3",
    "electron-builder": "^26.0.12"
  }
}
